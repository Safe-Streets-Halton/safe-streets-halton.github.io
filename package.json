{
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./_sass/input.css -o ./assets/style.css --watch",
    "jekyll": "bundle exec jekyll serve --watch"
  },

  "devDependencies": {
    "tailwindcss": "^4.0.0"
  }
}
